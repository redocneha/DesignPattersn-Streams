package structural;

//we share the objects that are already created when requested for new one, t
//objects are immutable here
//we check the map for requ obj type, if present return , else create one and add in map
//diff from prototype as in prototype, we still create objs by cloning and can be mutated, but here objs are not even created bt shared and immutable
public class Flyweight {
}
